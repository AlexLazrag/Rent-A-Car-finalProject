spring:
  server:
    error:
    include-message: always
    include-binding-errors: always
  datasource:
    url: jdbc:mysql://localhost:3306/rent_a_car?createDatabaseIfNotExist=true
    username: root
    password: 1234ssdd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
